apiVersion: v1
kind: Service
metadata:
  name: {{ include "cybershield.fullname" . }}
  labels:
    {{- include "cybershield.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cybershield.selectorLabels" . | nindent 4 }}
